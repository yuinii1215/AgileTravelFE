const state = {
  user: {
      name:"student",
      id:"1"
  }
}

// getters
const getters = {
  currentUser: state => state.user
}

// actions
const actions = {

}

// mutations
const mutations = {

}

export default {
  state,
  getters,
  actions,
  mutations
}